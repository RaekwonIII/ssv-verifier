2026-04-22 Iteration 1
- Reviewed the repo state, issue list, and dependency order.
- Selected issue #1: Bootstrap CLI With Network Selection because it is the first unblocked task.
- Created branch `ralph/issue-1-bootstrap-cli-network-selection`.
- Bootstrapped a TypeScript CLI with validated env loading, `hoodi|mainnet|both` selection, docs, and a basic automated test.
- Verified the slice with `npm test` and `npm run build`.
- Closed issue #1 after pushing commit `d67ae6c`.
- Started issue #2: added a `verify-network` health check command for RPC reachability, subgraph `_meta`, and deployed bytecode at the configured `Views` address.
- Expanded automated coverage to include successful and failing `verify-network` runs.
- Verified the slice with `npm test` and `npm run build`.
- Created branch `ralph/issue-3-verify-cluster-identity` for iteration 2.
- Selected issue #3: Verify Cluster Identity For One Cluster as the next unblocked feature.
- Added a `verify-cluster` command, single-cluster subgraph fetch support, and a `Views` client that uses on-chain hashed-state validation to verify owner, operator IDs, validator count, and active status.
- Added automated coverage for successful and failing `verify-cluster` flows.
- Verified the slice with `npm test` and `npm run build`.
2026-04-22 Iteration 3
- Created branch `ralph/issue-4-derive-cluster-balance`.
- Selected issue #4: Verify Derived Current Cluster Balance as the next unblocked feature.
- Extended `verify-cluster` to fetch operator and DAO accounting inputs, derive the current cluster balance at the latest RPC block, and compare that value against `Views.getBalance(...)`.
- Added regression coverage for the balance derivation formula plus successful and failing end-to-end balance comparisons.
- Verified the slice with `npm test` and `npm run build`.
- Created branch `ralph/issue-5-verify-cluster-liquidation`.
- Selected issue #5: Verify Liquidation State For One Cluster as the next unblocked feature.
- Extended `verify-cluster` to derive burn rate and liquidation collateral, then compare burn rate and liquidatable status against `Views` using the same block-context as the balance check.
- Added regression coverage for liquidation formulas plus successful and failing liquidation comparison paths.
- Verified the slice with `npm test` and `npm run build`.
- Reviewed open issues #5 through #12 and kept work scoped to the next unblocked feature in dependency order.
- Re-ran `npm test` and `npm run build` on branch `ralph/issue-5-verify-cluster-liquidation` before closing issue #5.
- Created branch `ralph/issue-6-json-report-output`.
- Selected issue #6: Add JSON Report Output For Single-Cluster Checks as the next unblocked feature.
- Added `--output text|json` for `verify-cluster`, keeping the existing human-readable summary and exposing the full result payload as structured JSON.
- Added automated coverage for JSON output parsing plus successful and failing JSON result payloads.
- Verified the slice with `npm test` and `npm run build`.
- Created branch `ralph/issue-7-batch-verify-network`.
- Selected issue #7: Batch Verify All Clusters On One Network as the next unblocked feature.
- Added a `verify-clusters` command that fetches all cluster ids for one network, reuses the single-cluster verifier, and reports aggregate pass/fail totals plus failing cluster ids.
- Added automated coverage for batch aggregation and CLI parsing.
- Verified the slice with `npm test` and `npm run build`.
- Created branch `ralph/issue-8-batch-verify-both-networks`.
- Selected issue #8: Support Batch Verification Across Both Networks as the next unblocked feature.
- Extended `verify-clusters` so `--network both` runs the existing per-network batch verifier for `hoodi` and `mainnet`, then reports per-network and combined totals.
- Added automated coverage for the combined multi-network summary path.
- Verified the slice with `npm test` and `npm run build`.
- Created branch `ralph/issue-9-lag-aware-classification`.
- Selected issue #9: Add Lag-Aware Result Classification as the next unblocked feature.
- Extended `verify-cluster` to capture subgraph `_meta` freshness, compare it against the current chain head, and downgrade lag-affected mismatches from hard failures to warnings in both text and JSON output.
- Updated batch aggregation to track warned checks separately from true failures.
- Added automated coverage for a lag-affected warning path and a fresh non-lag mismatch path.
- Verified the slice with `npm test` and `npm run build`.
- Selected issue #10: Verify Operator State End To End as the next unblocked feature.
- Added a `verify-operator` command that compares subgraph operator fee, validator count, and active status against `Views.getOperatorFee(...)` and `Views.getOperatorById(...)`.
- Added automated coverage for successful and failing operator verification flows.
- Verified the slice with `npm test` and `npm run build`.
- Selected issue #11: Verify DAO And Network Config End To End as the next unblocked feature.
- Added a `verify-config` command that compares subgraph DAO values against `Views` network fee and liquidation parameter getters.
- Added automated coverage for successful and failing DAO/network config verification flows.
- Verified the slice with `npm test` and `npm run build`.
- Selected issue #12: Add Fixture-Backed Regression Tests For Verification Flows as the final open feature.
- Added fixture-backed regression coverage for the single-cluster verification path using stable pass and fail snapshots under `test/fixtures/verify-cluster/`.
- Verified the fixture regressions run under the main `npm test` command alongside `npm run build`.
2026-04-23 Iteration 1
- Reviewed the current repository state and the newly opened GitHub issues #22 and #24 through #35.
- Selected issue #25: Introduce inconclusive Results And Unified Exit Policy as the next unblocked foundational slice.
- Added a shared status module that centralizes aggregate status priority as `fail > inconclusive > warn > pass` and applies the unified exit policy where `warn` stays zero-exit while `fail` and `inconclusive` are non-zero.
- Updated command aggregation to reuse the shared status policy and extended batch cluster summaries to count inconclusive checks explicitly.
- Added automated coverage for aggregate status priority, exit-code policy, and inconclusive-aware batch summaries.
- Verified the slice with `npm test` and `npm run build`.
2026-04-23 Iteration 3
- Reviewed the current repository state, progress log, and all open GitHub issues.
- Selected issue #29: Fix Single-Operator Verification With Dual ETH/SSV Fees as the next unblocked feature.
- Created branch `ralph/issue-29-dual-operator-fees`.
- Updated `verify-operator` to read the correct subgraph operator fields, derive user-facing `active` from `removed`, and compare both ETH and SSV fee surfaces against the asset-aware Views adapter.
- Marked missing directly comparable subgraph operator fields as `inconclusive` instead of misreporting them as failures.
- Added automated coverage for passing, failing, and inconclusive operator verification scenarios.
- Verified the slice with `npx vitest run test/verify-operator.test.ts`, `npm test`, and `npm run build`.
- Selected issue #30: Add verify-operators Batch Command as the next unblocked feature.
- Created branch `ralph/issue-30-verify-operators`.
- Added a public `verify-operators` command that lists all subgraph operators on one or both networks, runs single-operator verification for each item, and continues through per-operator errors as inconclusive batch results.
- Added aggregate batch summaries for operator counts and non-passing operator details by network, plus CLI parsing and routing for `verify-operators`.
- Added automated coverage for single-network and dual-network batch operator verification, mixed-status aggregation, and CLI parsing.
- Verified the slice with `npx vitest run test/verify-operators.test.ts test/network-selection.test.ts`, `npm test`, and `npm run build`.
2026-04-23 Iteration 4
- Reviewed the current repository state, progress log, and all remaining open GitHub issues, then selected issue #31 as the next unblocked feature in dependency order.
- Created branch `ralph/issue-31-network-constants` in a separate worktree from the pushed `#30` commit to avoid the live `.ralph` state-file edit in the original worktree.
- Replaced `verify-network` health semantics with dual-surface network-constants verification that compares ETH and SSV DAO values against the asset-aware `Views` adapter for `hoodi`, `mainnet`, and `both`.
- Added grouped human-readable output by asset type per network, JSON output with explicit asset-qualified check names, and updated the CLI help and README to match the new command behavior.
- Expanded automated coverage for passing and mixed dual-surface verification results, then verified the slice with `npx vitest run test/network-selection.test.ts test/verify-config.test.ts`, `npm test`, and `npm run build`.
- Selected issue #32: Verify Cluster Asset Type And ETH Effective Balance as the next unblocked feature after closing #31.
- Created branch `ralph/issue-32-cluster-asset-type` from the verified `#31` slice.
- Extended `verify-cluster` to read subgraph `feeAsset` and `effectiveBalance`, infer the accepted on-chain asset type by probing both ETH and SSV `Views` surfaces, and fail hard on asset-type mismatches before downstream accounting checks run.
- Added an explicit ETH `effectiveBalance` prerequisite check and marked deeper ETH accounting checks inconclusive until the follow-up dual-surface balance slice lands, while preserving the existing SSV verification path.
- Expanded cluster regression coverage for asset-type mismatch handling, ETH-only effective-balance behavior, and the updated dual-surface call ordering, then verified the slice with `npx vitest run test/verify-cluster.test.ts test/verify-cluster-fixtures.test.ts`, `npm test`, and `npm run build`.
- Selected issue #33: Compare Cluster Balance And Burn Rate Across ETH/SSV Paths as the next unblocked feature after closing #32.
- Created branch `ralph/issue-33-dual-cluster-balance-burn-rate` from the verified `#32` slice.
- Extended cluster accounting fetches and normalization to carry both ETH and SSV operator and DAO fee-index surfaces, then selected the correct surface from the verified cluster asset type.
- Updated `verify-cluster` to derive and compare current balance plus burn rate against the matching ETH or SSV `Views` methods, while keeping ETH liquidation checks deferred to the follow-up liquidation issue.
- Expanded automated coverage for ETH passing and failing balance-or-burn-rate scenarios alongside the existing SSV regression paths, then verified the slice with `npx vitest run test/verify-cluster.test.ts test/verify-cluster-fixtures.test.ts`, `npm test`, and `npm run build`.
- Selected issue #34: Fix liquidationCollateral And liquidatable Cluster Checks as the next unblocked feature after closing #33.
- Created branch `ralph/issue-34-liquidation-collateral-checks` from the verified `#33` slice.
- Corrected `verify-cluster` so `liquidationCollateral` now compares subgraph-derived collateral against independently derived Views-side collateral inputs, while `liquidatable` remains the subgraph-side decision compared against the on-chain ETH or SSV liquidatable result.
- Extended the liquidation flow across both ETH and SSV asset paths by reading the matching Views liquidation constants and keeping the check semantics aligned with their names.
- Expanded automated coverage for ETH liquidation collateral mismatches alongside the existing SSV liquidation regression paths, then verified the slice with `npx vitest run test/verify-cluster.test.ts test/verify-cluster-fixtures.test.ts`, `npm test`, and `npm run build`.
- Selected issue #35: Add Inconclusive-Aware Batch Summaries For Clusters as the final open implementation slice under the PRD.
- Created branch `ralph/issue-35-cluster-batch-inconclusive` from the verified `#34` slice.
- Updated `verify-clusters` to continue through per-cluster verification errors by wrapping them as inconclusive batch results instead of aborting the whole run.
- Added JSON rendering for cluster batch runs so both text and machine-readable output include explicit inconclusive totals and per-cluster results.
- Expanded automated coverage for mixed batch results that include thrown per-cluster errors, then verified the slice with `npx vitest run test/verify-clusters.test.ts`, `npm test`, and `npm run build`.
2026-04-25 Iteration 3
- Reviewed the current repository state, progress log, and all open GitHub issues #49 through #68, then selected issue #49 as the next unblocked feature in dependency order.
- Created branch `ralph/issue-49-cluster-id-parser` from `sprint-2`.
- Added a shared cluster ID parser that validates lowercased owner addresses, allowed operator-set sizes (`4|7|10|13`), canonical positive operator IDs, strict ascending order, and uniqueness before verification starts.
- Tightened CLI parsing so `verify-cluster` rejects `--network both`, and command-specific `--cluster` and `--operator` flags now fail fast when used with the wrong command.
- Added regression coverage for canonical cluster ID parsing plus malformed owner, operator count, operator formatting, ordering, uniqueness, and irrelevant CLI flag combinations.
- Verified the slice with `npx vitest run test/verify-cluster.test.ts test/network-selection.test.ts`, `npm test`, and `npm run build`.
2026-04-25 Iteration 4
- Reviewed the current repository state, progress log, and all open GitHub issues #48 and #50 through #68, then selected issue #50 as the next unblocked slice after #49.
- Created branch `ralph/issue-50-pinned-cluster-fetcher` from the local `#49` tip.
- Added a structured pinned cluster fetch path that loads `_meta.block.number` and the target cluster snapshot together, keeps primary/fallback source tracking, and returns explicit `success`, `not-found`, or `query-failed` outcomes instead of relying on exceptions for expected cases.
- Updated cluster verification to consume the new pinned fetch result while preserving the existing higher-level command behavior until the follow-up `clusterState` rewrite lands.
- Added focused regression coverage for pinned fetch success, not-found with verification-block context, fallback success, and all-sources query failure.
- Verified the slice with `npx vitest run test/subgraph.test.ts test/verify-cluster.test.ts test/verify-cluster-fixtures.test.ts`, `npm test`, and `npm run build`.
